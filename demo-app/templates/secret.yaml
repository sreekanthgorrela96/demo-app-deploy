apiVersion: v1
kind: Secret
metadata:
  name: {{ include "demo-app.fullname" . }}
  labels:
    {{- include "demo-app.labels" . | nindent 4 }}
type: Opaque
stringData:
  API_KEY: {{ .Values.secret.apiKey | quote }}
